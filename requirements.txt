# Ledger Pilot — runtime + test dependencies (Python 3.12)
# Install:  python -m venv .venv && .venv\Scripts\pip install -r requirements.txt

# engine
pandas>=2.2
scikit-learn>=1.5
numpy>=1.26
PyYAML>=6.0
openpyxl>=3.1          # .xlsx statements via pandas.read_excel

# persistence — SQLite by default, Postgres via DATABASE_URL (docker-compose.yml)
SQLAlchemy>=2.0
psycopg[binary]>=3.2

# api
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9   # UploadFile on /api/clients/{cid}/upload
pydantic>=2.8

# tests
pytest>=8.0
httpx>=0.27            # fastapi.testclient, for the posting-safety tests

# Tier 3 production adapter is optional — engine/llm.py imports it lazily:
#   pip install anthropic
